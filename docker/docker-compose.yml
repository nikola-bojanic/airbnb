version: '3.9'
services:
  postgres:
    container_name: postgres
    image: postgres:16
    environment:
      POSTGRES_DB: airbnb
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - 5432:5432
    volumes:
      - postgres_data:/var/lib/postgresql/data
volumes:
  postgres_data: